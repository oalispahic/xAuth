{
  "name": "xauth-web",
  "version": "0.1.0",
  "private": true,
  "description": "xAuth web gate: login page, OTP submission and the nginx auth_request check.",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=../dev/xauth.env --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
